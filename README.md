* train a model and track experiments with mlflow
* register and promote model versions (staging → production)
* serve predictions via fastapi
* containerize with docker and push to docker hub
* deploy to k3s homelab cluster
* gitops with argocd — push to git, auto-redeploy
* github actions
* monitor with prometheus + grafana